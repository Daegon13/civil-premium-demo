# Patch Plan

1. Eliminar boilerplate de `create-next-app` y dejar base limpia.
2. Crear árbol de carpetas para una landing premium escalable.
3. Agregar tokens iniciales simples de color y spacing.
4. Definir metadata básica del sitio en `layout.tsx`.
5. Dejar `page.tsx` como composición de secciones placeholder.
6. Documentar arquitectura y plan dentro de `docs/`.
